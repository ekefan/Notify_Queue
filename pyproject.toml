[project]
name = "notify-queue"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.19.1",
    "asyncpg>=0.31.0",
    "fastapi[standard]>=0.141.1",
    "sqlalchemy[asyncio]>=2.0.52",
]


[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["src"]